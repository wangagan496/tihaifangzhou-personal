# 题海方舟代码导览与面试定位

> 这份文档用于让面试官或协作者快速找到项目的核心实现。它只描述仓库当前源码已存在的能力；“已识别问题”和“改进方案”不等同于已上线修复。

## 30 秒阅读顺序

1. `entryability/EntryAbility.ets`：应用启动、保存 Context、初始化主题、加载 `pages/Index`。
2. `pages/Index.ets`：四个一级 Tab，并保存 UIContext，供网络错误跳转和工具类使用。
3. `common/utils/request.ets` + `Auth.ets`：Token 注入、业务码判断、401 清理登录态、受保护页面登录回跳。
4. `views/project/Project.ets` + `views/interview/Interview.ets`：项目分类题库、面经搜索排序，以及共享列表组件的参数化复用。
5. `views/home/QuestionListComp.ets` + `pages/QuestionDetailPage.ets`：题目/面经列表、详情、上一条/下一条、点赞收藏和内容类型路由。
6. `common/utils/Tracking.ets`：普通题阅读时长本地队列；面经详情直接上报停留时间和完整阅读状态。
7. `views/mine/Mine.ets` + `views/mine/MineProfileHeader.ets` + `MineStatsGrid.ets` + `MineToolsList.ets`：我的页容器、资料区、统计区和工具区；再结合 `common/utils/MineQuestionStore.ets` 看用户隔离记录。
8. `pages/ProfileEditPage.ets` + `common/utils/ProfilePolicy.ets` + `pages/AccountSettingsPage.ets` + `pages/MessageSettingsPage.ets`：资料更新、账号边界、通知权限和偏好持久化。
9. `pages/AudioPage.ets` + `views/AudioPage/*` + `common/utils/AudioDB.ets`：权限、录音、沙箱文件、RDB 元数据和播放释放。

## 项目结构

| 位置 | 当前职责 | 面试可讲点 |
| --- | --- | --- |
| `entry/src/main/ets/entryability/EntryAbility.ets` | Stage 模型入口；写入应用 Context、初始化主题、加载首页 | UIAbility 生命周期、启动链路 |
| `entry/src/main/ets/pages/Index.ets` | 组织首页/项目/面经/我的四个 Tabs；保存 UIContext | 一级入口用 Tabs；二级页面用 Router |
| `entry/src/main/ets/common/utils/request.ets` | Axios 实例、Bearer Token、业务码、401 统一处理 | 统一请求入口和出口、鉴权边界 |
| `entry/src/main/ets/common/utils/Auth.ets` | 用户状态持久化、登录判断、登录后返回目标页 | AppStorage/PersistentStorage、受保护路由 |
| `entry/src/main/ets/views/project/Project.ets` | 加载项目分类卡片，并按分类展示项目题目 | 分类接口与题目列表组件复用 |
| `entry/src/main/ets/views/interview/Interview.ets` | 面经搜索、排序和列表展示 | 查询条件状态与共享列表组件参数化 |
| `entry/src/main/ets/views/home/QuestionListComp.ets` | 题目/面经列表、刷新、分页、空状态、重试和详情前鉴权 | `questionBankType`、内容类型复用；`isRequesting` 防重复请求 |
| `entry/src/main/ets/pages/QuestionDetailPage.ets` | 题目/面经详情、点赞收藏分享、上一条/下一条和两套阅读埋点 | 路由内容类型驱动 UI 与接口参数 |
| `entry/src/main/ets/views/mine/Mine.ets` | 我的页容器、生命周期和学习时长请求 | 页面组合、状态与功能组件边界 |
| `entry/src/main/ets/views/mine/MineProfileHeader.ets` | 头像、登录状态、编辑资料和打卡入口 | `@Prop` 数据传递、登录保护 |
| `entry/src/main/ets/views/mine/MineStatsGrid.ets` | 历史记录、收藏、点赞和累计学时入口 | GridRow 统计区、路由参数和学时格式化 |
| `entry/src/main/ets/views/mine/MineToolsList.ets` | 常用词、录音、分享、反馈、关于和设置入口 | 整行点击区域、Router 和 Share Kit |
| `entry/src/main/ets/common/utils/MineQuestionStore.ets` | 按用户 ID 读写历史记录、收藏和点赞列表 | Preferences 本地镜像；没有服务端“我的列表”接口 |
| `entry/src/main/ets/pages/MineQuestionsPage.ets` | 展示历史记录、我的收藏和我的点赞，并回到题目详情 | 路由参数区分列表模式；空状态与登录保护 |
| `entry/src/main/ets/pages/MineFeedbackPage.ets` | 输入并保存最近一次意见反馈 | 当前保存到本机 Preferences，不冒充服务端提交 |
| `entry/src/main/ets/pages/MineAboutPage.ets` | 展示应用名称、版本和功能说明 | 独立二级页面，使用 Navigation 返回 |
| `entry/src/main/ets/pages/AccountSettingsPage.ets` | 展示当前账号、资料入口、协议入口和退出登录 | 登录态展示、受保护路由和账号操作边界 |
| `entry/src/main/ets/pages/MessageSettingsPage.ets` | 消息偏好开关、系统通知权限和测试通知 | Notification Kit、Preferences 持久化 |
| `entry/src/main/ets/common/utils/MessageSettingsStore.ets` | 保存消息分类及声音振动偏好 | 页面与 Preferences 存储职责分离 |
| `entry/src/main/ets/pages/ProfileEditPage.ets` | 修改昵称和头像，并同步更新本地用户状态 | 表单状态、头像上传和局部资料更新 |
| `entry/src/main/ets/common/utils/ProfilePolicy.ets` | 合并资料接口返回值，保留未更新的用户字段 | 纯函数、局部更新和状态一致性 |
| `entry/src/main/ets/common/utils/Tracking.ets` | Preferences 队列、累计 5 条批量上报 | 成功才清空、失败保留；切题前先保存当前记录 |
| `entry/src/main/ets/pages/AudioPage.ets` | 麦克风运行时授权与拒绝后的产品兜底 | 声明权限 + 运行时授权 |
| `entry/src/main/ets/views/AudioPage/AudioRecordComp.ets` | AVRecorder、沙箱 `.m4a` 文件、文件描述符和录音状态 | 录音资源生命周期、退出页面清理 |
| `entry/src/main/ets/views/AudioPage/AudioPlay.ets` | AVPlayer 播放、停止、释放播放器和 FD | 避免播放器与文件句柄泄漏 |
| `entry/src/main/ets/common/utils/AudioDB.ets` | RDB 元数据表、按用户查询、按创建时间排序 | 大文件放沙箱；RDB 只存路径和元数据；用户隔离 |

## 重点面试链路

### 1. 网络鉴权

```text
业务页面 → tgRequest() → 请求拦截器读取 auth.getUser().token
→ Authorization: Bearer token → 服务端
→ 响应拦截器判断业务码
→ 401：清理本地用户状态并 replaceUrl 到登录页
```

- Token 的注入和业务错误由网络层统一处理，页面只消费业务数据并处理自身 UI 状态。
- 受保护页面通过 `auth.checkAuth()`：未登录时带上目标 URL 和参数进入登录页，登录成功后再返回。

### 2. 项目与面经列表复用

```text
Project / Interview
→ 组装分类、搜索或排序条件
→ QuestionListComp
→ questionBankType 区分项目题与面经
→ 统一处理请求、刷新、分页、空状态和详情跳转
```

- `Project.ets` 先加载项目分类，再把选中的 `typeId` 传给通用列表。
- `Interview.ets` 只维护关键词和排序状态，通过 `questionBankType: 9` 复用同一列表组件。
- 列表组件只负责列表生命周期和通用交互，详情页根据内容类型选择对应的参数和埋点。

### 3. 阅读时长

```text
普通题详情成功 → tracking.start(questionId)
→ 切题或离开详情页 → tracking.save()
→ Preferences 队列累计 5 条 → 批量上报
→ 服务端成功清空；失败保留

面经详情成功 → 记录进入时间
→ 滚动到底标记完整阅读 → 切换或离开时
→ POST /hm/interview/tracking(questionNo, stayTime, exp)
```

普通题继续复用本地队列和批量上报；详情页在连续切题前会先保存当前记录，且仅在有效 `start` 后执行 `save`。面经使用接口要求的独立埋点结构，离开时按秒上报停留时间，并通过 `exp` 表示是否滚动读到底。

### 4. 我的页与本地题目记录

```text
进入我的页 → 历史记录/收藏/点赞 → MineQuestionsPage
题目列表或详情加载/操作成功 → MineQuestionStore
→ Preferences(mineQuestions) 按 userId 保存 → 点击记录返回题目详情
```

- 历史记录在题目加载完成后写入；点赞和收藏在服务端操作成功后同步本地列表，避免接口失败时 UI 先显示成功。
- `MineQuestionStore` 只负责客户端本地镜像，不代表已经存在服务端“我的收藏/我的点赞”查询接口；切换用户时通过 `userId` 读写不同键，避免串数据。
- 推荐分享由 `MineToolsList.ets` 调用 `common/utils/MineShare.ets`，使用 `@kit.ShareKit` 打开系统分享面板；意见反馈由 `MineFeedbackStore.ets` 保存到本机 `feedback` Preferences，关于我们是独立说明页。

### 5. 账号与消息设置

```text
Mine → 资料编辑 / 账号设置 / 消息推送
→ ProfileEditPage / AccountSettingsPage / MessageSettingsPage
→ Auth、ProfilePolicy、MessageSettingsStore
→ AppStorage 或 Preferences 持久化
```

- 资料编辑只更新接口返回的头像或昵称，`ProfilePolicy` 合并时保留 token、用户编号等未变字段。
- 账号设置集中展示当前登录态，并复用 `auth.checkAuth()` 保护资料入口；退出账号后回到 `Index`。
- 消息页区分系统通知权限和应用内偏好，偏好写入 `messageSettings`，并提供测试通知验证链路。

### 6. 录音与本地存储

```text
进入录音页 → 请求麦克风权限 → 创建沙箱 m4a 和文件描述符
→ AVRecorder prepare/start → stop/release/关闭 FD
→ 读取文件大小 → 写入 RDB 元数据 → AVPlayer 按路径播放
```

- 音频是二进制大文件，放在应用沙箱；RDB 只保存路径、时长、大小、创建时间和用户 ID。
- 查询必须按 `user_id` 过滤并按 `create_time` 排序，避免切换账号后串录音数据。
- 页面退出时会停止并释放录音器/播放器，关闭文件描述符。

## 当前已识别的边界（如被问到时的说法）

1. **面经埋点失败重试**：当前离开页面时直接上报，失败不会像普通题队列一样持久化重试；可补充本地待上报队列。
2. **删除录音一致性**：文件系统和 RDB 不在同一事务中；需要文件删除、数据库删除与补偿/重试策略协同。
3. **保存录音孤儿文件**：若音频生成后 RDB 写入失败，应补偿删除文件或记录待清理标记。

## 建议面试演示顺序

1. 用 README 或本文件讲 45–60 秒项目介绍。
2. 打开 `request.ets`，讲 Token、业务码与 401。
3. 打开 `Project.ets`、`Interview.ets` 和 `QuestionListComp.ets`，讲项目/面经如何复用一套分页列表并通过接口参数区分内容。
4. 打开 `Tracking.ets` 和 `QuestionDetailPage.ets`，讲普通题批量计时与面经单次埋点。
5. 依次打开 `Mine.ets`、`MineProfileHeader.ets`、`MineStatsGrid.ets`、`MineToolsList.ets`，再看 `MineQuestionStore.ets` 和 `MineQuestionsPage.ets`，讲组件职责、本地记录和用户隔离边界。
6. 打开 `ProfileEditPage.ets`、`AccountSettingsPage.ets`、`MessageSettingsPage.ets`，补充资料合并、登录保护和通知偏好持久化。
7. 最后打开 `AudioPage.ets`、`AudioRecordComp.ets`、`AudioDB.ets`，讲权限、文件、数据库和资源释放。

## 运行与检查

在 DevEco Studio 打开仓库根目录并同步依赖；选择 `entry` 模块后运行或构建 HAP。构建前请使用自己的签名配置，不要提交 Token、密码、私钥或生产签名材料。
